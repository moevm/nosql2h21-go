// import classes from 'core/go/';

type GoBoardProps = {
    message: string;
}

const ExComponent = (props: GoBoardProps) => {
    return (
        <div>

        </div>
    );
}

export default ExComponent;