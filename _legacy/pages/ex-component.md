```tsx

type ExComponentProps = {
    message: string;
}

const ExComponent = ({ message }: ExComponentProps) => {
    return (
        <div>
            {message}
        </div>
    );
}

export default ExComponent;

```
